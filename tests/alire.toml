name = "lml_tests"
description = "Tests for the built-in Alire test runner"
version = "0.0.0-test"

[[depends-on]]
lml = "*"

[[pins]]
lml = { path = ".." }

[build-profiles]
lml       = "validation"
lml_tests = "validation"
