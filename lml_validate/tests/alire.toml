name = "lml_validate_tests"
description = "Tests for the built-in Alire test runner"
version = "0.0.0-test"

[[depends-on]]
lml_validate = "*"

[[pins]]
lml_validate = { path = ".." }

[build-profiles]
lml_validate       = "validation"
lml_validate_tests = "validation"
